# Runtime data & state (server-only, never commit)
history/
uploads/
memory/
skills/
reminders/
reminders.json
reminders.test.tmp.json
followups.json
pending.json
choices.json
choices.test-*.json
cal_notified.json
data/quiz-state.json
data/avot-state.json
quiz-paused.flag
health-state.json
heartbeat
*.tmp
*.log

# Secrets and per-user config (server-only)
.env
access.json

# Claude state — but keep the project permissions file
.claude/*
!.claude/settings.json

# Dependencies / build
node_modules/
.buildcheck/

# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
voice-replies-off.flag
voice-pending.json
pending.test-*.json
choices.asktest-*.json
*.test.tmp.json

remind.clitest-*.json
followups.clitest-*.json
wal-salvage-*.txt

# tool cache: contains the local username and full folder layout
.impeccable/
